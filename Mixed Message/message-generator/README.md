# Message Generator (JavaScript + Node)

A random message generator built in JavaScript.
Each run prints a new message composed of multiple random parts.

## Run locally

You need Node.js installed.

```bash
node index.js